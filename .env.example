# API-Football (api-sports.io) — free-tier key from https://dashboard.api-football.com
# Copy this file to .env.local and paste your key. .env.local is gitignored.
#
#   cp .env.example .env.local   &&   edit .env.local
#
# Server-only (no NEXT_PUBLIC_ prefix) — never exposed to the browser.
API_FOOTBALL_KEY=
